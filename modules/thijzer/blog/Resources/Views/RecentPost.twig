<div>
  <h3 class="widget-title">{{ 'lbl.recent.post'|lng|capitalize }}</h3>
  <ul>
    {% for title in titles %}
    <li><a href="{{ meta.site }}blog/article/{{ title.id }}/{{ title.uri }}">{{ title.title }}</a></li>
    {% endfor %}
  </ul>
</div>
